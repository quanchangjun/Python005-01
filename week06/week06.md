# week06

1. 类和对象

   ~~~ python
   #python中把类也定义成对象，一切皆对象。比如列表、元组、字典等对象，对象有自己的功能。
   #对象是一个数据以及相关行为的集合
   #类的两大成员：属性和方法
   #类属性和对象属性
   #初始化函数，使用def __init__(self)来进行实例化，得到实例化对象,self表示实例对象本身
   #如果区分是类中静态字段还是实例中普通字段，可以使用__dict__来查看属性
   
   作用域：
   _name 人为约定不可修改
   __name 私有属性
   __name__ 魔术方法
   
   
   方法：
   普通方法 至少一个self参数，表示该方法的对象
   类方法 至少一个cls参数，表示该方法的类
   静态方法 由类调用，无参数
   
   在方法上可以加两种语法糖，
   一种是classmethod，类方法
   一种是staticmethod，静态方法
   #当类需要一系列构造函数时候，就可以使用classmethod
   @classmethod 语法糖装饰器
   __new__() 构造函数
   
   #classmethod作为构造函数来使用
   1、放在父类中定义classmethod，子类可以使用
   2、函数调用类并返回类的时候
   
   #staticmethod，额外实现一些功能，实现一些和类和实例不相关的
   #静态方法可以由类直接使用
   #因为不传入self，也不传入cls，所以不能使用类属性和实例属性
   
   #属性拦截工具
   #__getattribute__
   #__getattr__
   #对于不存在的属性，执行顺序：__getattribute__ --> __getattr__ --> __dict__
   
   property属性描述符，本质是特殊类
   #property
   #property可以直接当函数使用，也可以作装饰器使用（将方法封装成属性）
   #@property
   #另一种propety写法。
   #gender = property(get_,set_,del_,'other property')
   #property本质并不是函数，而是特殊类（实现了数据描述符的类）
   #如果一个对象同时定义了__get__()和__set__()方法，则称为数据描述符
   #如果仅定义了__gte__()方法，则称为非数据描述符，比如__getattribute__()
   
   
   
   
   ~~~

   

2. 封装、继承、重载、多态

   ~~~
   经典类
   新式类，所有的类都继承自父类，或者叫基类，即继承了object类（object）
   
   .mro()方法
   有向无环路图
   ~~~

   

3. solid设计原则与设计模式&单例模式

   ~~~
   单一责任原则
   大类拆分成小类
   
   开放封闭原则
   里氏替换原则
   
   依赖倒置原则
   高层模块不应该依赖低层模块
   
   接口分离原则
   
   单例模式
   对象只存在一个实例
   __init__和__new__的区别：
   __new__是实例创建之前被调用，返回该实例对象，是静态方法
   __init__是实例对象创建完成后被调用，是实例方法
   __new__先被调用，__init__后被调用
   __new__的返回值（实例）将传递给__init__方法的第一个参数，__init__给这个实例设置相关参数
   
   ~~~

4. 工厂模式

5. 元类

   元类是创建类的类，是类的模板

6. 抽象基类

   ~~~
   #抽象基类用来确保派生类实现了基类中的特定方法
   #mixin
   
   
   ~~~

   